---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: onepassword
spec:
  provider:
    onepassword:
      connectHost: http://onepassword-connect:8080
      vaults:
        Homelab: 1
      auth:
        # Token is managed out-of-band.
        secretRef:
          connectTokenSecretRef:
            name: onepassword-connect-token
            namespace: secrets
            key: token
